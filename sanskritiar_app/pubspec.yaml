name: sanskritiar_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # REPLACED the old plugin with the new, maintained version
  ar_flutter_plugin_2: ^0.0.3

  # Using the latest, compatible versions of other plugins
  permission_handler: ^11.3.1
  geolocator: ^12.0.0
  flutter_compass: ^0.8.0
  http: ^1.2.1
  provider: ^6.1.2
  vector_math: ^2.1.4
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/